// Tailwind v4 is configured through CSS (`@theme` in app/globals.css) rather
// than a JavaScript config file, so this is the whole of the build setup.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
